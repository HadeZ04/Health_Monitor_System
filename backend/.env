# Backend Environment Variables
# Copy this file to .env and update the values

# Server Configuration
PORT=4000
JWT_SECRET=HadezD
JWT_TTL=4h

# Inference Server URL
# Nếu inference server chạy local: http://localhost:8080
# Nếu inference server chạy trên GPU remote: http://your-gpu-server-ip:8080
# Nếu có domain: https://your-domain.com
INFERENCE_API_URL=http://localhost:8080

# MQTT (optional - chỉ cần nếu dùng MQTT)
# MQTT_URL=mqtt://localhost:1883
