# Backend Environment Variables
# Copy this file to `backend/.env` (optional) and update values.
#
# If you run via `docker-compose.yml`, use the repo root `.env.example` instead.

# ======================
# Server
# ======================
NODE_ENV=development
PORT=4000

# ======================
# Database
# ======================
# Local Postgres:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/health_monitor?schema=public
#
# Docker compose uses `db` as hostname:
DATABASE_URL=postgresql://postgres:postgres@db:5432/health_monitor?schema=public

# ======================
# JWT
# ======================
# Secrets must be >= 32 characters.
JWT_SECRET=your-super-secret-jwt-key-min-32-characters-long
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=your-super-secret-refresh-key-min-32-characters-long
JWT_REFRESH_EXPIRES_IN=30d

# ======================
# CORS
# ======================
CORS_ORIGIN=http://localhost:3000

# ======================
# Chatbot
# ======================
# Local Qwen router server (see repo root `qwen_router_server.py`)
# If backend runs in Docker and Qwen runs on host (Windows/macOS), use:
# QWEN_API_URL=http://host.docker.internal:8081
QWEN_API_URL=http://localhost:8081

# Gemini API key (fill later)
GEMINI_API_KEY=

